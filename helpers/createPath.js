// const fs = require("fs").promises;
// const path = require("path");

// const createPath = async (...arg) => {
//   return await fs.readFile(
//     path.join(__dirname, "..", ...arg.join().replaceAll(",", "/")),
//     "utf8"
//   );
// };

// module.exports.createPath = createPath;
